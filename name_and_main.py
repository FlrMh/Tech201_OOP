# __name__ and __main__

# set before anything else happens in python
# __name__ = __main__ when we run the file
# when we run the code in its file, it will print __main__; when we run the same code in a different file, instead of __main__ it will print the actual set name of the file.